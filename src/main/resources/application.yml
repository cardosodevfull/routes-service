spring:
  datasource:
    diverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/db-local
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: false
